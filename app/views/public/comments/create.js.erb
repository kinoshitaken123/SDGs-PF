$('#comment_area').html("<%= j(render 'public/product_comments/index', product: @product) %>");
$('#comment_form').html("<%= j(render 'public/product_comments/form', product: @product, comment: @new_comment) %>");

<!--#発火した際のtext_fieldを空白に更新するように指定-->
$("textarea").val('')